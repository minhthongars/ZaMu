<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:focusable="true"
    android:clickable="true"
    android:paddingTop="7dp"
    android:paddingHorizontal="16dp"
    android:foreground="?android:attr/selectableItemBackgroundBorderless">

    <androidx.cardview.widget.CardView
        android:id="@+id/cv_track_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:cardElevation="0dp"
        app:cardCornerRadius="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_track_avatar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/tv_order"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/grey_50"
            android:scaleType="centerCrop"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="28sp"
            android:textStyle="bold"
            tools:text="1"/>

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Take care of you đi"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="@color/color_text_secondary"
        android:textStyle="bold"
        android:textSize="12sp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="26dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/cv_track_image"
        app:layout_constraintTop_toTopOf="@id/cv_track_image"
        app:layout_constraintBottom_toTopOf="@id/tv_cut_info"/>

    <TextView
        android:id="@+id/tv_cut_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        tools:text="Võ văn minh thông"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@color/color_text_secondary"
        android:textSize="12sp"
        app:layout_constraintStart_toStartOf="@id/tv_name"
        app:layout_constraintTop_toBottomOf="@id/tv_name"
        app:layout_constraintEnd_toEndOf="@id/tv_name"
        app:layout_constraintBottom_toBottomOf="@id/cv_track_image"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/grey_200"
        android:layout_marginTop="7dp"
        app:layout_constraintTop_toBottomOf="@id/cv_track_image"/>

    <ImageView
        android:id="@+id/iv_close"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:src="@drawable/media3_icon_playlist_remove"
        android:padding="2dp"
        android:foreground="?attr/selectableItemBackground"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/grey_500" />

</androidx.constraintlayout.widget.ConstraintLayout>